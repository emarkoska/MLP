Run `python setup.py install` to install `mlp` module, which is going to be used through the tasks.

Open this directory and run `./start.sh`, which downloads the data and runs the notebook via `iPython`.
